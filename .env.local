# Backend en la maquina, para desarrollo. El emulador de Android no ve
# `localhost` como el host: `10.0.2.2` es su alias.
#   flutter run --dart-define-from-file=.env.local
API_BASE_URL=http://10.0.2.2:3000/api/v1
